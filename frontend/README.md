# Frontend

Next.js (App Router, TypeScript, Tailwind CSS v4).

Full-stack run instructions are in the **repository root** `README.md`.

```bash
npm install
npm run dev
```

Environment: `API_URL` in `.env` and `.env.local` (server-side fetch to FastAPI).
